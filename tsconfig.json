{
	"include": ["**/*.ts", "**/*.tsx", ".claude/**/*.ts"],
	"compilerOptions": {
		/* Language target and environment */
		"target": "ES2024",
		"lib": ["ES2024", "DOM", "DOM.Iterable"],
		"jsx": "react-jsx",
		"types": ["vite/client"],
		"paths": { "#/*": ["./src/*"] },

		/* Bundler mode: Vite transpiles, TypeScript only type-checks */
		"module": "preserve",
		"moduleResolution": "bundler",
		"moduleDetection": "force",
		"noEmit": true,
		"esModuleInterop": true,
		"resolveJsonModule": true,
		// `src/prisma/*.ts` imports each other with explicit `.ts` extensions —
		// required by the Prisma CLI's Node-native loader, which runs contract.ts
		// outside Vite's bundler resolution. Vite/tsc both tolerate the extension.
		"allowImportingTsExtensions": true,
		"verbatimModuleSyntax": true,
		"isolatedModules": true,
		"erasableSyntaxOnly": true,
		"skipLibCheck": true,

		/* Strictness */
		"strict": true,
		"noImplicitOverride": true,
		"noUncheckedIndexedAccess": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"noFallthroughCasesInSwitch": true,
		"noUncheckedSideEffectImports": true
	}
}
