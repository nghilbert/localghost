# Overlay (merged via COMPOSE_FILE) granting the app container AMD GPU access for
# the cookbook hardware panel. Untested (dev box is NVIDIA); confirm rocm-smi is
# reachable in-container. Omit on CPU-only hosts.
services:
  web: &gpu
    devices:
      - /dev/kfd
      - /dev/dri
  web-dev: *gpu