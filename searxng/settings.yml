# Drop ahmia/torch: Tor-only engines that error without a Tor proxy.
use_default_settings:
  engines:
    remove:
      - ahmia
      - torch

# Enable the JSON API (stock image is html-only). Limiter/secret are env-driven.
search:
  formats:
    - html
    - json
